# AGENTS.md — 서브에이전트 가이드

> 이 레포에서 어떤 일에 어떤 서브에이전트를 쓰는지 정리. CLAUDE.md를 보완하는 문서.

---

## 1. 기본 원칙

- 작업을 시작하기 전에 적합한 서브에이전트가 있으면 먼저 그쪽으로 위임한다.
- 메인 에이전트는 컨텍스트가 비싸므로, 코드 탐색·리뷰·반복 작업은 가능한 한 서브에이전트가 처리한다.
- 서브에이전트는 메인 대화 컨텍스트를 보지 못한다. 호출 시 작업 목적·이미 시도한 것·기대 결과를 자기완결적으로 적어 준다.
- 독립적인 여러 작업을 동시에 시킬 수 있으면 **한 메시지에서 병렬 호출**한다.

## 2. 작업 유형별 권장 서브에이전트

| 작업 유형 | 권장 에이전트 | 언제 |
|---|---|---|
| 코드 위치 찾기 / 심볼 검색 | `Explore` | "X가 어디서 정의됐는지", "Y 패턴을 쓰는 파일들" |
| 광범위한 코드베이스 조사 | `general-purpose` | 3개 이상의 grep / 파일 읽기가 필요한 조사 |
| 설계 / 구현 전략 수립 | `Plan` | 다중 단계 / 다파일 변경의 사전 설계 |
| 코드 리뷰 / 변경 검증 | `general-purpose` 또는 ultrareview | PR 직전, 큰 변경 직후 |
| Claude API / Claude Code 사용법 질의 | `claude-code-guide` | Claude Code 훅 / MCP / 설정 / Anthropic SDK 질의 |

> 위 에이전트 목록은 이 환경에 사전 등록된 것을 기준으로 한다. 환경에 따라 다를 수 있다.

## 3. 위임 vs 직접 처리 기준

**직접 처리한다**
- 단일 파일 읽기 / 수정 (Read / Edit / Write)
- 짧은 grep 한두 번
- 명령 한두 줄 실행

**서브에이전트에 위임한다**
- 다수 파일에 걸친 패턴 탐색
- "이 시스템이 어떻게 동작하는지" 같은 개방형 조사
- 큰 리팩토링의 사전 계획 수립
- 독립적으로 진행 가능한 병렬 작업 두 건 이상

## 4. 위임 시 프롬프트 패턴

서브에이전트에 위임할 때 다음 구조로 적어 준다:

```
목적: <왜 이 일을 시키는지>
이미 알고 있는 것: <탐색 안 해도 되는 영역>
기대 결과: <어떤 형태의 답을 받고 싶은지, 분량 제한>
```

응답 분량 제한 (`200자 이내`, `리스트만`, `파일 경로만`)을 두면 컨텍스트를 절약한다.

## 5. 이 프로젝트에서 자주 발생할 위임 시나리오

| 시나리오 | 위임 대상 |
|---|---|
| "slides-grab의 CLI 명령 중 X가 무엇을 하는지 vendor/ 안에서 찾아줘" | `Explore` |
| "Next.js 라우트 X에서 발생하는 데이터 흐름을 따라가 줘" | `general-purpose` |
| "Phase 1 T3 구현 전략 (DB 스키마 적용 방식) 설계 좀" | `Plan` |
| "사용자 facing 텍스트에 영문 기술 용어 남아있는지 전 페이지 훑어 줘" | `Explore` (`grep`) |

## 6. 서브에이전트가 만지면 안 되는 영역

- `PRD.md` — 어떤 에이전트도 직접 편집 금지.
- `docs/mockups/0X-*.html` — UI 기준 문서. 코드 변경 중 부수적으로 mockup을 임의 수정하지 않는다. mockup 변경은 사용자 합의 후 메인 에이전트가 명시적으로 처리.
- 사용자 workspace 폴더 (`data/` 바깥) — 사용자 슬라이드를 함부로 만지지 않는다.
